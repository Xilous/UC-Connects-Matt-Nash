<Window x:Class="PM_Project_Tracking.OrderingHwByJobSelect"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:staticData="clr-namespace:PM_Project_Tracking"
        Title="OrderingHwByJobSelect" Height="800" Width="740">
    <Window.Resources>
        <staticData:PermissionTier x:Key="PermissionTier" />

        <SolidColorBrush x:Key="SolidBorderBrush" Color="#888" />
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FF494949" />
        <SolidColorBrush x:Key="SelectedBrush" Color="#FF575555" />
        <SolidColorBrush x:Key="BillingGridBorderBrush" Color="#FF8DB258" />
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid>
                            <Border Name="Border" Background="{StaticResource BackgroundBrush}">
                                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="12,2,12,2" RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF494949">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Label Content="" Height="60" Foreground="White"/>
        </Grid>
        <Grid Grid.Row="1">
            <DataGrid Name="DG_AvawareProjList"
                AutoGenerateColumns="False"
                CanUserSortColumns="False"
                CanUserAddRows="False"
                RowHeaderWidth="0"
                ColumnHeaderHeight="45"
                VirtualizingStackPanel.IsVirtualizing="True" 
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                CanUserDeleteRows="True"
                SelectionMode="Single" MouseDoubleClick="DG_AvawareProjList_MouseDoubleClick" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="50" Binding="{Binding Id}" IsReadOnly="True" >

                    </DataGridTextColumn>
                    <DataGridTextColumn Header="JOB NUMBER" Width="90" Binding="{Binding ProjectNumber}" IsReadOnly="True" >

                    </DataGridTextColumn>
                    <DataGridTextColumn Header="PROJECT NAME" Width="*" Binding="{Binding ProjectName}" IsReadOnly="True" >

                    </DataGridTextColumn>
                    <DataGridTextColumn Header="IMPORTED DATE" Width="80" Binding="{Binding ImportedDate}" IsReadOnly="True" >

                    </DataGridTextColumn>
                    <DataGridTextColumn Header="IMPORTED TIME" Width="80" Binding="{Binding ImportedTime}" IsReadOnly="True" >

                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
